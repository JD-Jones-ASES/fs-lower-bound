# Disclosure

AI-generated results with human managing the workflow. Final report generated by Claude Code (Fable 5) with contributions from Codex (GPT 5.6 Sol) and Grok Build (Grok 4.6).

Every numerical ingredient is machine-checked, and the verification path requires no trust in any of these systems. There are three ways to check the result, in ascending effort. Run `verify.py`: standard library only, seconds to finish. It rebuilds the arc sets from their definition, validates both certificates, checks the Paley chains, and recomputes the constant. Or read the one new lemma's one-page proof together with Krachun's Lemmas 4 and 5, which are used here unchanged. Or reimplement the construction from the README's definitions, which state everything needed.

Independent verification is invited. Questions and verification reports are welcome via GitHub issues.
